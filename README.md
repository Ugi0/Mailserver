# Tokkicorp mailserver

This is a small project I made to allow inviting other people to use my mailserver using registration codes. It's mostly made for fun, but does actually function as a fully functional mail server

## Dependencies

The actual mail functionalities are done using [docker-mailserver](https://github.com/docker-mailserver/docker-mailserver), but the mailbox management and creation can be done using my wrapper.
